import React from 'react';
import {Text, View} from 'react-native';

const Notes = () => {
  return (
    <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
      <Text>I am the notes screen </Text>
    </View>
  );
};

export default Notes;
